# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/home/blaine/.config/i3blocks/blocklets/$BLOCK_NAME
separator_block_width=15

[cpu_usage]
label=CPU
interval=10
align=center

[memory]
interval=5

[netinfo]
color=#859900
interval=10

[vpn]
label=VPN
interval=10

[keymap_switcher]
label=KB
interval=once

[audio]
label=VOL
instance=Master
interval=1
signal=10

[brightness]
label=SCR
signal=12
interval=once

[battery]
label=BAT
instance=0
interval=60

[battery]
label=BAT2
instance=1
interval=60

[time]
interval=1
